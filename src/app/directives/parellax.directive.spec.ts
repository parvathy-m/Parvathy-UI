import { ParellaxDirective } from './parellax.directive';

describe('ParellaxDirective', () => {
  it('should create an instance', () => {
    const directive = new ParellaxDirective();
    expect(directive).toBeTruthy();
  });
});
